{
	"name": "lex-pr-runner",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"engines": {
		"node": "20.19.5",
		"npm": "10.8.2"
	},
	"packageManager": "npm@10.8.2",
	"bin": {
		"lex-pr": "dist/cli.js"
	},
	"scripts": {
		"build": "tsup src/cli.ts --format esm,cjs --dts --out-dir dist && tsup src/mcp/server.ts --format esm,cjs --dts --out-dir dist",
		"check-schemas": "npm run generate-schemas && git diff --exit-code schemas/",
		"cli": "tsx src/cli.ts",
		"cli:built": "node dist/cli.js",
		"dev": "tsx watch src/cli.ts",
		"format": "echo 'Format: using editorconfig for consistent formatting'",
		"generate-schemas": "tsx scripts/generate-gate-schema.ts",
		"lint": "echo 'Lint: TypeScript compilation handles most issues' && npm run typecheck",
		"mcp": "tsx src/mcp/server.ts",
		"test": "vitest",
		"test:run": "vitest run",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.18.2",
		"chalk": "^5.3.0",
		"commander": "^12.0.0",
		"execa": "^9.2.0",
		"fs-extra": "^11.2.0",
		"simple-git": "^3.24.0",
		"yaml": "^2.6.0",
		"zod": "^3.23.8"
	},
	"devDependencies": {
		"@types/node": "^20.12.12",
		"eslint": "^9.10.0",
		"tsup": "^8.1.0",
		"tsx": "^4.19.0",
		"typescript": "^5.6.3",
		"vitest": "^2.0.0",
		"zod-to-json-schema": "^3.24.6"
	}
}
